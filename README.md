# Calculator App

A simple command-line calculator application that allows users to perform addition, subtraction, multiplication, and division. The application validates user input and handles errors such as division by zero.

## Project Structure

